-- Optional configurations for code/project stats tracking plugins
-- Currently available: codestats.net, wakatime (wakapi.dev)

return {
  -- Stats tracking is disabled in the 'clean' branch variant
}
